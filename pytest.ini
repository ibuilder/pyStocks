[pytest]
# The upstream Kronos clone (gitignored, optional) ships its own tests + heavy
# deps; keep it out of our collection.
norecursedirs = vendor_kronos build build_release dist dist_release .git
addopts = --ignore=vendor_kronos
testpaths = tests
