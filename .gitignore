# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# PyInstaller build artifacts
build/
dist/
build_release/
dist_release/
installer/Output/
*.manifest
*.log
assets_icon_preview.png

# Local data / secrets
.env
.stockpredict_cache/
.cache/

# Kronos experiment: keep forecast/ (harness + wrapper + vendored model code),
# drop the full upstream clone and raw eval outputs (evidence lives in KRONOS_EVAL.md).
vendor_kronos/
kronos_*.txt
kronos_*.csv

# OS
Thumbs.db
.DS_Store
